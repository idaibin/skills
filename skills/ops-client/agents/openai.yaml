interface:
  display_name: "Ops Client"
  short_description: "Verify desktop apps without disrupting user sessions"
  default_prompt: "Use $ops-client to verify a specified real desktop client or perform only an explicitly authorized client action through an available platform adapter, applying the screen-session safety gate and returning bounded direct evidence without source-code changes."
