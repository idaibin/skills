interface:
  display_name: "Ask ChatGPT"
  short_description: "Coordinate bounded ChatGPT collaboration"
  default_prompt: "Use $ask-chatgpt for requested ChatGPT input: infer the outcome, prefer the Codex App-native Project/thread route unless I specify a surface, send only with exact authorization, preserve idempotency and attribution, and verify the result locally."
