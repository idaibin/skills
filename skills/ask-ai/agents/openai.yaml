interface:
  display_name: "Ask AI"
  short_description: "Build and route external AI reviews and visuals"
  default_prompt: "Use $ask-ai to build an evidence-based external AI review or image request, select only the explicitly authorized provider and capability, prefer an available host image tool when no provider is named, and verify all results locally."
