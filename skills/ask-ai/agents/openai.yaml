interface:
  display_name: "Ask AI"
  short_description: "Coordinate bounded external AI collaboration"
  default_prompt: "Use $ask-ai for requested external AI input: resolve only authorized providers, freeze one basis, verify provider-specific routes and targets, submit each operation once, preserve independent attribution, and verify all results locally."
