interface:
  display_name: "Frontend Implementation"
  short_description: "Implement frontend UI while preserving app contracts"
  default_prompt: "Use $frontend-implementation to implement or review frontend UI, routes, forms, tables, dashboards, responsive behavior, or architecture while preserving the existing stack, design system, state, API contracts, and verification flow."
