interface:
  display_name: "Repository Map"
  short_description: "Map architecture, dependencies, guidance, and authority"
  default_prompt: "Use $repo-map to resolve real roots and maintain or explicitly rebuild the smallest current semantic map, separating source/build/runtime identities while preserving verified reuse and owner-bounded repair."
