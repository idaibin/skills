interface:
  display_name: "Repository Map"
  short_description: "Scan, query, and render repository asset graphs"
  default_prompt: "Use $repo-map to scan, query, or render the smallest bounded repository asset graph, preserving source/build/runtime identity separation, coverage, conflicts, and unresolved edges without authoring repository guidance or changing source."
