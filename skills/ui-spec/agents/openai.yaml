interface:
  display_name: "UI Specification"
  short_description: "Specify traceable UI targets and visual evidence"
  default_prompt: "Use $ui-spec to freeze the selected source, separate source targets from current runtime, write traceable Feature Spec deltas, and update root DESIGN.md only when shared visual semantics change."
