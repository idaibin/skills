# Eval Cases

Use these cases when changing `dev-frontend` triggers, stack guidance, layout-ownership rules, validation expectations, or metadata.

## Contents

- Trigger Eval
- Non-Trigger Eval
- Independent Review Outlet Eval
- UI Documentation Fixtures
- Quality Eval
- Scoring

## Trigger Eval

| User prompt | Expected result | Why |
| --- | --- | --- |
| `Fix this React page without changing the layout.` | Should trigger `dev-frontend`. | Targeted frontend implementation with layout constraint. |
| `Add this Vue 3 settings form using the existing script setup, Pinia, Router, and component patterns.` | Should trigger `dev-frontend`. | Vue-native implementation and state ownership. |
| `Fix this Vue SFC's watchEffect request loop, provide/inject ownership, temporary Router guard, and keep-alive cancellation without converting its Options API style.` | Should trigger `dev-frontend`. | Vue-native reactivity, component contracts, Router lifetime, and cleanup. |
| `Fix the schedule admin form against the supplied backend contract. Before editing, find the existing selector and representative caller, then prove the POST path, changed body fields, enabled-only create selector, all-status filter, lazy organization lookup trigger, and response success rule; stop on any unresolved decisive conflict.` | Should trigger `dev-frontend`, complete the bounded owner/reuse search, then freeze the API contract and task-local revision before editing. | Prevents parallel implementation against guessed parameters, trigger timing, or duplicate components. |
| `Fix the local loading/error rendering around this page's existing API call. Do not change its request or response contract.` | Should trigger `dev-frontend`; inherit the current verified API owner and freeze only the changed UI-state semantics and focused check. | A small API-backed UI fix must not be blocked by unrelated contract archaeology. |
| `The user corrected the accepted request field after a worker returned an old implementation and one old hunk already landed.` | Create a new task-local freeze revision, reject the delayed old-revision result, inventory and reconcile the landed old hunk, then resume against the corrected revision. | A correction must close both in-flight and already-landed stale work. |
| `Fix this pure Options API component using data, computed, watch, this.$watch, activated/deactivated, and beforeRouteLeave; do not add Composition imports.` | Should trigger the Vue Options API branch. | Native Options reactivity and lifecycle. |
| `Add this form to the existing AntD page using current patterns.` | Should trigger `dev-frontend`. | Component-system consistency. |
| `Replace this React component's mutually exclusive compact, expanded, and overlay booleans with the repository's existing variant or compound composition without changing behavior.` | Should trigger the React component-composition branch. | Invalid prop-state combinations require a scoped implementation change. |
| `Migrate this package from ESLint to Oxlint while preserving type-aware rules, ignores, editor, CI, and non-mutating checks.` | Should trigger `dev-frontend` with explicit toolchain migration evidence. | Frontend lint implementation, not generic review. |
| `Convert this legacy .eslintrc and .eslintignore to flat config without changing the linted file set, overrides, processors, or CI flags.` | Should trigger `dev-frontend` with a distinct flat-config migration comparison. | Flat config has different ignore, dotfile, glob, and CLI semantics from an Oxlint migration. |
| `Fix this Vite mode config without exposing secrets through VITE_* variables.` | Should trigger `dev-frontend` and preserve the actual build/deploy modes. | Authorized frontend toolchain implementation. |
| `Wire this page to the real backend route; the dev proxy, production gateway context path, auth source, and loading/error/permission states differ.` | Trigger `dev-frontend` with project grounding before edits. | The frontend change crosses API, runtime, auth, and delivery owners. |
| `Update this Tailwind v4 shadcn component using its CSS-first theme and components.json; preserve local edits.` | Should trigger Tailwind plus shadcn profiles. | Version-aware source-owned component change. |
| `Build this admin table page using the existing filters, row actions, and empty/error states.` | Should trigger `dev-frontend`. | Admin UI implementation with existing patterns. |
| `Refactor this admin page: six business tabs currently keep their APIs, forms, tables, and drawers in one route component. Reuse the existing navigation, form, table, popup, and selector components.` | Should trigger `dev-frontend`, complete the reuse-first gate, keep a thin page shell, assign each independently stateful tab a feature owner, and add a focused ownership invariant when the repository has a credible test seam. | Multi-tab business ownership and reuse are implementation concerns, not a reason to create a parallel component system. |
| `This Vite page has messy imports and component organization; clean only what is needed.` | Should trigger `dev-frontend`. | Frontend organization without broad refactor. |
| `Simplify this component: too many nested divs and repeated CSS rules.` | Should trigger `dev-frontend`. | DOM and CSS simplification in frontend code. |
| `Clean up this page structure: shell, content container, animation wrapper, and page root are all fighting overflow.` | Should trigger `dev-frontend`. | Layout ownership and DOM boundary cleanup. |
| `Merge this animation wrapper into the semantic page root and keep one scroll owner.` | Should trigger `dev-frontend`. | Animation wrapper and scroll ownership cleanup. |
| `Move these split-pane widths out of JSX utilities into one layout class.` | Should trigger `dev-frontend`. | Business geometry ownership cleanup. |
| `Replace h-[22px] and w-[88px] with project scale utilities or named CSS owners.` | Should trigger `dev-frontend`. | Tailwind arbitrary pixel cleanup. |
| `Use the existing Tailwind style conventions; do not redesign the page.` | Should trigger `dev-frontend`. | Styling convention preservation. |
| `Implement this React and Tailwind table using the existing variants, spacing scale, query state, and responsive conventions.` | Should trigger React plus Tailwind profiles. | Combined framework and styling implementation. |
| `Add this Vue Composition and Pinia settings flow while preserving Router, cancellation, and keep-alive ownership.` | Should trigger Vue Composition with existing Pinia ownership. | Combined framework and state implementation. |
| `Fix this Vue Options and Ant Design form without adding Composition helpers or replacing its controls.` | Should trigger Vue Options plus Ant Design profiles. | Combined framework and component-system implementation. |
| `Fix this Tauri settings UI, but keep native commands behind IPC.` | Should trigger `dev-frontend`. | Desktop webview UI implementation. |
| `Align this React app to its documented route, component, store, script, and file-naming standard.` | Should trigger `dev-frontend`. | Explicit frontend structure alignment. |
| `Flatten these nested divs, use one flex owner for centering, and keep the children adaptive.` | Should trigger `dev-frontend`. | Minimal DOM and Flex ownership. |
| `Remove repeated CSS and stop the component from duplicating the page container padding.` | Should trigger `dev-frontend`. | CSS and outer-spacing ownership. |
| `The page shell already owns the 24px inset; remove the standard panel's duplicate left/top padding while preserving its internal control spacing and keep the scrollbar flush right.` | Trigger `dev-frontend` with layout governance; trace effective insets by axis, keep one owner per boundary, and verify the target plus an affected sibling if the shell is shared. | Nested padding ownership is a source and runtime layout change. |
| `Apply the accepted redesign direction: keep one primary accent role, preserve semantic status colors, and add dark mode only for the surfaces named by DESIGN.md.` | Trigger `dev-frontend` with the visual-direction protocol; keep Preserve/Overhaul explicit and do not introduce a second accent system or universal dark theme. | Visual direction and theme semantics are materially changing. |
| `Before creating this page, inspect existing routes and components and reuse the closest implementation.` | Should trigger `dev-frontend`. | Reuse-first page development. |
| `Use the nearest existing component as a reference and justify any new file.` | Should trigger `dev-frontend`. | Reference-first component development. |
| `Migrate this React feature to the generated OpenAPI client, preserve nullability, auth, loading/success/error, and remove its touched duplicate DTO.` | Should trigger `dev-frontend`. | Protocol client consumption is frontend source work. |
| `Upgrade this app to Vite 8, migrate applicable Rollup options/plugins to Rolldown, and verify production output.` | Should trigger `dev-frontend` as an explicit build migration. | Build configuration and source validation are requested. |
| `Remove the declarations this React refactor makes obsolete, but preserve dynamically registered routes and exports.` | Should trigger `dev-frontend` with shared code-quality and React reachability rules. | Scoped implementation cleanup. |
| `Update this page's existing native API wrapper and types; the repository has no generated contract pipeline.` | Should trigger `dev-frontend` and mark protocol automation `Not applicable`. | Frontend API work does not itself require OpenAPI. |
| `Fix this role picker so loading, failed, successful-empty, cached refresh failure, and populated states stay distinct; retry is inside the form.` | Should trigger `dev-frontend`. | Async state semantics and form-button behavior require source implementation. |
| `Show updater failure without replacing the archive task's current status; keep both domains independently actionable.` | Should trigger `dev-frontend`. | Independent async state owners need separate feedback composition. |
| `Replace this decorative delayed table-row animation with immediate state feedback, preserve reduced motion, and keep rapid keyboard navigation interruptible.` | Trigger `dev-frontend` and load interaction/motion quality after resolving existing contracts and tokens. | Motion and feedback behavior are part of the authorized source change. |
| `Implement the approved account slice using this repository's requirements source, resolved design-root DESIGN.md, and matching UI contract.` | Should trigger `dev-frontend`, resolve authorities by meaning, and consume them directly. | Existing product and UI contracts guide authorized source work without another specification pass. |
| `This API document replaces the repository's older copy; update the authorized local contract first, then implement the frontend.` | Freeze the incoming artifact identity, replace the declared local authority before code work, and reject conclusions based on the superseded copy. | Explicit replacement must be reflected in the selected authority basis. |
| `Implement this approved account page with a product Feature Spec, a selected-source UI Feature Spec, and resolved design-root DESIGN.md.` | Read each applicable authority separately before live source/config; do not use either Feature Spec as a substitute for the other. | Product behavior and UI implementation acceptance have distinct owners. |
| `Implement this approved table with resolved design-root DESIGN.md, existing Tailwind config, and no repo map.` | Should read the contract before live config/source, verify the adapter binding, and proceed with targeted discovery. | A repo map is navigation, not an implementation gate. |
| `A longer localized label now wraps beside the primary action; keep intermediate widths usable and verify the rendered geometry.` | Should trigger `dev-frontend` and load layout governance with proportional runtime evidence. | A small copy change can affect task completion through wrapping and state geometry. |
| `Implement this admin slice using the Feature Spec's required desktop viewport, optional secondary desktop viewport, and excluded mobile viewport.` | Consume the declared viewport acceptance matrix without copying its schema; test required, select optional only when useful, and do not add mobile acceptance or infer mobile is unsupported. | Viewport scope belongs to the accepted slice contract rather than a universal responsive checklist. |
| `Run the project build, but if it rewrites tracked generated files outside this task, keep them out of the change.` | Snapshot and compare Worktree state around validation, classify the drift, and restore only task-owned side effects with a known exact pre-state. | A nominal validation command may mutate the checkout. |
| `While the build is rewriting a tracked generated file, another owner adds a hunk to that same file.` | Preserve the combined diff, do not restore or stage it, stop, and hand mixed/concurrent ownership to `repo-review`; prefer isolated generation when the writer is known in advance. | A known pre-state cannot prove exclusive ownership after concurrent writes. |
| `Move this page Drawer to the app shell. It must block the page, allow its nested participant picker, work from both entrypoints, and restore focus when closed.` | Freeze the shell/route/entrypoints/open state, reuse the existing overlay owner, and verify outer mask/background blocking, both entrypoints, nested overlay stacking and clipping, dismissal, and focus restoration on the target surface. | Page-level overlay ownership changes can pass a standalone preview while failing nested interaction in the real host. |
| `The embedded drag still fails after the initial runtime check and one correction recheck, but the standalone preview passes. Keep patching.` | Stop the implementation loop, preserve the current diff, freeze the embedded surface and direct expected/observed evidence, and return to diagnosis or a single-owner handoff. Keep the preview result as separate evidence. | A second unchanged target-surface failure is a stop condition rather than permission for speculative patches or surface substitution. |

## Non-Trigger Eval

| User prompt | Expected result | Why |
| --- | --- | --- |
| `Understand this repository's real commands and directory structure first.` | Should prefer `repo-map`. | Repository mapping. |
| `Plan the frontend rewrite across three apps before anyone edits code.` | Should not trigger this Skill; use the host's built-in planning. | Future cross-scope planning. |
| `Find the root cause before changing any code.` | Should not trigger this Skill; use the host's built-in diagnosis under effective instructions. | Diagnosis before implementation. |
| `Review all dirty changes and propose commit groups.` | Should prefer `repo-review`. | Dirty-tree review and staging plan. |
| `Audit this frontend architecture for duplicated components, state boundaries, accessibility, and performance.` | Should prefer `audit-frontend`. | Read-only domain audit. |
| `Does this Vite app need Rolldown configuration? Audit only; do not edit.` | Should prefer `audit-frontend` Build/Tooling. | Read-only toolchain assessment. |
| `Verify this page in the browser and check console/network.` | Should prefer `ops-browser`. | Runtime browser evidence. |
| `Capture the real Electron app window with platform-specific window evidence.` | Should prefer `ops-client`. | Desktop-client evidence. |
| `These frontend changes are reviewed; stage, commit, and push them.` | Should prefer `repo-delivery`. | Authorized Git mutation after review. |
| `Translate this selected visual source into the project UI-spec location against resolved <design-root>/DESIGN.md, but do not edit product source.` | Should prefer `ui-spec`. | UI specification ownership. |
| `Change one tooltip sentence where the existing container has ample adaptive space.` | Should trigger `dev-frontend` for the source edit but should not force the layout-governance profile without reachable geometry risk. | Layout validation is proportional, not universal ceremony. |
| `Change this API response type without touching rendered states, interaction feedback, transitions, gestures, or motion.` | Trigger the bounded frontend implementation but do not load interaction/motion quality. | A frontend file change alone is not a semantic motion trigger. |
| `Define the product behavior, permission rules, user-visible states, and acceptance before anyone edits source.` | Should prefer `product-spec`. | Product decisions are unresolved. |
| `Identify whether this existing Markdown file is a PRD or UI contract, but do not change source or specifications.` | Should not invoke `product-spec` or `ui-spec`; classify it from declared authority, path, content, links, and approval. | File recognition alone is not specification or implementation work. |
| `Change only a local CSS color token; reachable API, build, runtime, and cross-repository contracts stay unchanged.` | Keep project-grounding risk classes `Not applicable`; do not scan unrelated repositories or deployment state. | A styling-only source edit does not activate cross-project evidence work. |
| `Change one local CSS declaration. Do not change behavior, API, state, shared components, or contracts.` | Use the small-change fast path: one bounded lookup, one minimal patch, and current dev diagnostics plus diff inspection; do not add a red test or run a bare aggregate test/full build. | A one-owner styling edit does not justify TDD or repository-wide gates. |
| `Use the current repo-map snapshot to change a component shared by two pages.` | Make one bounded implementation-navigation query for the unresolved consumer edge, verify only decisive current source, freeze the path, implement, and stop after one completion checkpoint. Do not refresh/render the map. | A current graph can remove rediscovery but does not replace live-source authority. |
| `Implement this local component fix; no compatible repo-map snapshot exists.` | Continue with bounded live-source discovery. Do not scan or render a map as prerequisite ceremony. | Missing optional navigation must not lengthen a known implementation path. |
| `The current record already identifies this exact component and function. Change its local spacing; the running dev compiler is clean.` | Use the matched source directly, batch the edit, finish with local diff plus `git diff --check`, and report tests `Not verified`; do not call repo-map or start a new test/build. | A known clean micro change should not pay discovery or validation ceremony again. |

## Independent Review Outlet Eval

| User prompt | Expected result |
| --- | --- |
| `Implement the React slice, then explicitly prepare an independent ChatGPT implementation challenge without applying its findings.` | Keep `dev-frontend` as owner and emit one lightweight `ask-ai` handoff. |
| `Implement and validate the React slice locally; no external review was requested.` | Emit no `ask-ai` handoff. |

## UI documentation fixtures

These sanitized fixtures exercise the consumer boundary without carrying project
names, endpoints, props, slots, or visual values.

| Fixture | Expected consumer behavior |
| --- | --- |
| `current`: Product/UI/DESIGN/project-map Markdown and current source resolve | Read Markdown authorities, then verify routes, components, states, and consumers in live source. |
| `navigation-reference`: project-map names a route, component symbol, source owner, and representative consumer | Use it to navigate, then verify every reference in current source. |
| `copied-authority`: a derived UI map restates token values, DTO fields, component props/events, or behavior rules as its own facts | Keep the existing owner; do not copy or create a second contract. |
| `incomplete-lifecycle`: one or more of named owner, producer, non-LLM consumer, semantic version, executable validator, drift policy, or retirement rule is missing | Do not rely on the projection; continue from Markdown and current source and report `Not verified` without inventing lifecycle files. |
| `maintained-projection`: all seven lifecycle fields exist and its current non-mutating validator passes | Use it only for bounded navigation, then verify referenced routes, components, states, and consumers in live source. |
| `stale-map`: project-map points to a moved/removed route or component | Recheck bounded live source before editing; do not invent a replacement. |

## Quality Eval

| Case | Expected evidence | Reject if |
| --- | --- | --- |
| Validation proportionality | Batches a coherent slice, uses current dev/compiler diagnostics first, runs one smallest credible check at slice completion or on real failure, and reserves full gates for merge/release/deployment/final acceptance or explicit requests. | Validates after every edit, escalates because no focused check exists, runs a full gate for a bounded change, or uses a focused pass to claim release/runtime completion. |
| Sol coordination | Freezes owner, dependencies, write ownership, and acceptance once; dispatches independent packages together or sends a deterministic multi-stage chain to one execution agent/script, then reconciles once. | Alternates one successful operation, one wait, and one renewed reasoning pass or splits mutable state across writers. |
| Stack detection | Reads manifests/config/imports and reports actual framework and stack before choosing libraries or conventions. | Assumes a library or framework pattern is available because the user named it. |
| Specification authority | Reads only applicable declared product/shared visual/slice UI authorities, classifies them by meaning rather than filename, and invokes an owner only for a required unresolved decision. | Treats root `PRD.md` as universal, loads sibling specs, or invokes `ui-spec` merely because `DESIGN.md` exists. |
| DESIGN.md completeness consumer | Before a shared-system edit, requires producer status `awaiting-trusted-approval-verification` plus satisfied `forgeway-ui-design-completeness/1` claim `gate:ui-design-complete`, bound to the current approved design hash and same exact Result Package. | Treats lint zero, current CSS/theme, PackageManifest bytes, or a producer status rewritten to `complete` as complete shared authority. |
| Untrusted local DESIGN approval | Blocks `awaiting-trusted-approval-verification` without a satisfied consumer claim; requires a host-trusted approval receipt and the same exact Result Package before implementation, without rewriting the producer result. | Accepts applicant JSON/hash, a missing receipt, a receipt bound to a different package, or producer status rewritten to `complete`. |
| Feature Spec distinction | Separately reads applicable product requirements/product Feature Spec, selected-source UI Feature Spec, and resolved `<design-root>/DESIGN.md` before implementation facts. | Treats one Feature Spec as both product and UI authority or blocks a known scoped change on an optional absent artifact. |
| Design binding | Treats resolved `<design-root>/DESIGN.md` as shared semantics, then verifies Tailwind, component-library, or local-theme bindings in live config/source and reports drift or unexercised behavior honestly. | Promotes an adapter to shared authority, assumes automatic synchronization, or blocks solely because a repo map is absent. |
| Structured projection consumption | Requires all seven lifecycle fields, runs the existing non-mutating validator, preserves Markdown/source authority, and verifies referenced consumers in live source. | Trusts an unvalidated projection, hand-edits it, requires one for ordinary work, or treats it as product/UI/source authority. |
| Visual direction and anti-slop | Uses verified surface/audience/brand constraints, optional reasoned dials, explicit Preserve/Overhaul mode, one primary accent semantic role with state-color exceptions, and conditional dark-mode scope; rejects generic visual defaults only when the accepted direction or user impact supports it. | Applies a universal aesthetic, default dial values, mandatory dark mode, one literal color for every semantic state, or treats deliberate repeated layouts as slop. |
| Nested inset implementation | Traces shell → container → page → component → control, computes effective padding per axis, removes competing declarations, preserves internal component spacing, and verifies flush scrollbar plus sibling impact when a shared owner changes. | Patches one `px`/`pt`, stacks parent and child page insets, shifts the left/top grid, or leaks a page-specific inset model into siblings. |
| Interaction and motion quality | For a material motion/feedback change, states its communication purpose and frequency, reuses existing contracts/tokens, specifies transitioned properties instead of introducing `transition: all`, preserves reduced-motion/focus/hover capability, keeps rapid input interruptible, and obtains proportional runtime evidence or reports `Not verified`. | Activates from file extensions, invents exact timing/easing or a new visual system, adds decorative delay to frequent work, rewrites unrelated legacy motion, or claims feel/interaction quality from static checks. |
| Viewport acceptance | Consumes the applicable UI Feature Spec's required/optional/excluded viewport scope, honors newer explicit user constraints, and treats excluded entries only as outside current acceptance. | Recreates the matrix locally, expands to generic device coverage, skips required entries, or claims excluded viewports are unsupported. |
| Framework-native implementation | Preserves React hooks/effects or Vue SFC/API/composable/store/router conventions according to the detected project. | Applies React concepts to Vue, rewrites Vue API style incidentally, or imposes one framework pattern universally. |
| Evidence-gated cleanup | Avoids duplicate authorities and speculative layers, and removes only code made obsolete by the authorized change after framework/build reachability checks. | Broadly deletes text-search-or-lint “unused” code or refactors unrelated legacy smells. |
| Vite/Rolldown boundary | Detects actual versions; keeps Rolldown inside Vite for normal apps; treats Vite-major, `rolldown-vite`, option/plugin, SSR/library and output changes as explicit migrations with applicable build/runtime evidence. | Adds standalone Rolldown/config or labels an unaffected app a Vite 8 migration by default. |
| Vite env contract | Treats client prefixes as public bundle data and preserves parsing, mode precedence, ignored-local-file policy, and mode versus `NODE_ENV` behavior across applicable builds. | Places secrets in `VITE_*`, assumes typed values, or verifies only one unrelated mode. |
| Lint migration | Separates flat-config conversion from Oxlint adoption; preserves the actual linted file set and effective config, and verifies pinned type-aware tooling, TypeScript compatibility, unsupported rules, representative diagnostics, editor/CI adoption, and non-mutating checks until old/new parity is evidenced. | Retires the old gate because a migration generator, missing type-aware support, or syntax-only run passed, or treats Oxlint type-check as an automatic `tsc` replacement. |
| React quality | Preserves purity, immutable inputs, Rules of Hooks and effect semantics; uses memoization/code splitting only from repository evidence and measurement. | Enforces one component per file, named exports, shallow prop limits or default memoization as universal React rules. |
| React composition | Replaces proven mutually exclusive boolean modes or repeated coordinated assembly with an existing variant, slot, explicit component, or compound composition while keeping state at its owner. | Flags boolean count alone, converts independent flags, or introduces context/compound wrappers that add indirection without removing invalid states or repetition. |
| React side-effect boundary | Forbids non-local mutation during render while allowing necessary external-system updates in owned event handlers or Effects with correct dependencies, cleanup, and cancellation. | Bans all imperative SDK/subscription updates everywhere or permits mutation during render. |
| Vue SFC and API style | Preserves the repository's SFC layout and `<script setup>`, Composition API, or Options API choice for the touched component. | Converts API style incidentally or applies React hook/Context/effect rules to Vue. |
| Vue Options API | Uses `data`/`computed`/`watch` or owned `this.$watch`, native component/Router guards, and mounted/unmounted/activated/deactivated lifecycles without requiring Composition helpers. | Injects `ref`, `watchEffect`, `onScopeDispose`, or a `<script setup>` conversion into a valid pure Options component. |
| Vue API-native reactivity | For Composition, preserves `ref`/`reactive`/`computed`, handles reactivity escapes, gives `watch` an explicit source, and bounds `watchEffect` dependencies; for Options, preserves native `data`/`computed`/watch options or owned `this.$watch` without requiring Composition helpers. | Cross-applies the other API style, mirrors derivation through watchers, loses reactivity, or hides request loops, stale work, and post-`await` dependency assumptions. |
| Vue component and injection contracts | Preserves read-only props, explicit emits, `v-model` arguments/modifiers, slots, fallthrough behavior, and typed provide/inject keys, defaults, reactive ownership, and mutation authority. | Mutates props/injected state implicitly, changes payload/slot contracts, or creates an unowned injected singleton. |
| Vue store, Router, and lifetime | Keeps local state local, uses Pinia for established shared ownership, registers global/component guards at the correct owner, unregisters temporary guards, and cleans watchers/listeners/requests for scope, route, and keep-alive activation lifetimes. | Globalizes component state, duplicates guard/listener registration, relies on unmount for keep-alive cleanup, or lets stale requests win after navigation/deactivation. |
| Project-class alignment | Preserves framework-native routing and applies repository-defined toolchain, script, directory, and naming rules without incidental upgrades. | Forces one SPA layout across Next.js, Astro, Vue, Tauri, and other project classes. |
| Specification readiness | Reads available requirements, acceptance criteria, non-goals, affected contracts/files, and validation seams; uses host planning for unresolved complex work before editing. | Implements a complex ambiguous request without a usable specification or explicit assumptions. |
| API contract freeze | Searches current owners and reuse candidates before freezing; resolves only changed, depended-on, or acceptance-decisive API fields from the governing contract plus current call chain; lets unchanged non-decisive fields inherit their verified owner; stops on a decisive conflict; and uses a task-local revision to reject or reconcile stale work after a correction. | Freezes before finding the real owner, requires unrelated backend archaeology for a local UI-state fix, infers decisive fields from a screenshot or wrapper default, keeps work running against a superseded revision, or adds a fallback to hide unresolved authority. |
| Selected-source preflight | Reads source identity/approval, resolved design-root DESIGN.md, product/UI slice, traceable evidence and deltas; pauses on Partial/Not Ready, unavailable source, unresolved viewport/state, or missing P1 asset owner. | Starts from prose, a screenshot estimate, or current UI similarity without the controlling source gate. |
| Implementation mapping | Maps each applicable acceptance ID to owner file/component, reuse/extend/wrap/new, asset/data owner, and static/runtime verification before editing. | Codes first and reconstructs ownership or acceptance after the fact. |
| Source/runtime separation | Uses design inspect values as source-extracted targets and keeps browser-computed current values in the runtime column; enlarged visual checks remain inferred. | Treats current runtime geometry or asset size as the source target because it looks close. |
| Behavior-first slices | When a stable public seam exists, works one failing behavior check and minimal vertical slice at a time; skips TDD with a stated reason when only brittle implementation assertions are possible. | Writes horizontal test batches, tests private DOM/state details, or claims TDD without observing red before green. |
| Reuse and lifecycle | Reuses locally first and updates manifests, exports, routes, scripts, tests, CI/build/deploy paths, docs, indexes, and stale references for structural changes. | Extracts speculative shared packages or moves/deletes source paths without closing ownership records. |
| Layout preservation | Keeps layout, spacing, routes, copy, and visual system unchanged unless requested. | Redesigns or restyles adjacent UI. |
| Component consistency | Reuses existing components, wrappers, hooks/composables, services, stores, types, icons, aliases, and interaction helpers. | Introduces a parallel UI kit, helper layer, state layer, or icon library. |
| Existing implementation search | Uses an already matched same-file/owner/function record directly; otherwise performs one bounded live-source search or one existing-map query only for an unresolved cross-owner edge. | Reconfirms an exact match through repo-map, scans/renders a map for implementation, starts from a guessed path, or creates a file before checking existing code. |
| Map-assisted Sol path | Chooses once between direct one-owner lookup, one bounded query over a compatible snapshot, or bounded live-source fallback; verifies decisive paths, freezes the owner/chain, and reopens discovery only on contradictory source or a real focused-check failure. | Scans/renders for a local fix, repeats map/source discovery after freeze, trusts a miss as absence, or explores unrelated edges. |
| Reuse decision | Classifies candidates as direct reuse, reference-only, unrelated, or `Not found`; prefers reuse, then adaptation, then justified creation. | Duplicates an existing component or ignores the nearest established pattern. |
| Multi-section page ownership | Keeps route context, navigation, composition, and genuinely shared orchestration in a thin shell; assigns each independently stateful business tab/section its API, form, table, drawer, validation, and lifecycle owner; adds a focused ownership invariant when a credible seam exists. | Leaves independent business sections as large page-level conditional branches, duplicates shared owners per section, or enforces arbitrary line-count/component-per-file rules. |
| New-file placement | Explains why existing candidates are insufficient and follows the current directory, naming, props, state, styling, export, and test conventions. | Creates a parallel `shared`, `common`, `ui`, `components`, hook/composable, store, or service layer. |
| DOM minimality | Removes or avoids wrappers, fragments, components, and classes that do not provide layout, semantics, state, accessibility, animation ownership, or reuse value. | Adds nested wrappers only to pass class names or group a single child. |
| Layout path | Uses the shortest valid DOM path, prefers Flexbox for one-dimensional composition, and uses Grid only for genuine two-dimensional relationships. | Adds wrappers for simple alignment or uses multiple layout systems for one flow. |
| Alignment ownership | Applies horizontal/vertical alignment or centering at the owning parent without adding a centering-only child wrapper. | Repeats alignment across parent and child layers or centers content that should follow normal flow. |
| Adaptive children | Uses local grow/shrink/basis/wrapping/minimum-size conventions so children adapt to available space. | Hardcodes widths/heights or adds wrappers to compensate for missing flex constraints. |
| Layout ownership | Assigns shell chrome, content inset, page layout, panel bounds, and scroll/overflow to explicit owners without repeated competing rules. | Leaves multiple nested layers declaring the same height, padding, inset, width, or overflow behavior. |
| Outer-spacing ownership | Keeps page-edge margin/padding/inset at one shell, content, or page owner and limits reusable components to internal spacing. | Applies outer spacing in both the parent container and inner component. |
| Animation wrapper collapse | Merges page-transition wrappers into a semantic page root when the wrapper has no independent layout, state, accessibility, or reuse role. | Keeps an animation-only wrapper around an inner container that repeats page layout. |
| Scroll ownership | Keeps one main scroll region where practical and prevents headers, sidebars, inspectors, panels, and footers from competing for overflow control. | Uses broad overflow patches or nested scroll fixes to hide unclear ownership. |
| CSS minimality | Consolidates repeated declarations and relies on existing tokens, component props, cascade, and inheritance when safe. | Repeats CSS, restates defaults, or adds one-off overrides where existing styles work. |
| Tailwind scale discipline | Uses project scale utilities for routine sizing and spacing, and named owners for real product geometry. | Leaves routine sizes as arbitrary pixel utilities without justification. |
| Tailwind version/source fidelity | Detects v3/v4 configuration and keeps class names discoverable through the installed compiler or explicit source/safelist contract. | Requires one config model, builds dynamic class fragments, or applies version-specific dogma universally. |
| Repository-native stack detection | Detects the actual framework, component, styling, routing, state/data, form, icon, and build owners and loads only matching rules; treats variants as repository facts, not public modes. | Assumes a familiar stack, exposes one public mode per library, or cross-applies rules from an absent framework/system. |
| Common implementation report | Reports scope, detected boundaries, authorities/owners, selected frontend risks, changed files/contracts, validation, Worktree drift, excluded work, and `Not verified`; nests visual/layout/desktop evidence only when applicable. | Uses a stack-specific report shape, omits drift/exclusions, or requires visual fields for a non-visual task. |
| Component-system context | Preserves shadcn `components.json`/local generated ownership and Ant Design provider/static-feedback behavior for the installed major. | Regenerates over local changes, ignores consumers, or uses context-free static APIs by assumption. |
| Contract preservation | Traces and preserves route, query, payload, response, permission, loading, and error behavior. | Changes data contracts silently. |
| Grounding non-trigger | For a styling-only change with unchanged reachable data/build/runtime contracts, marks project-grounding risk classes `Not applicable`. | Scans backend repositories or requires every async/runtime state merely because the app has API code. |
| Async query-state integrity | Distinguishes initial pending/error from successful empty/populated data, preserves valid cached data during background refresh failure when supported, and uses explicit non-submit button types inside forms. | Defaults unknown data to empty before classifying state, hides valid cached data behind a refresh error, or lets retry submit the form. |
| Independent feedback ownership | Keeps primary-task and secondary-domain state owners separate unless an approved precedence contract combines them. | Multiplexes unrelated errors through one conditional status string and hides the user's current task state. |
| Protocol activation | Uses generated-client rules only for an existing pipeline or explicit migration/trial; otherwise preserves the repository-native API wrapper and DTO owner and reports `Not applicable`. | Introduces OpenAPI from REST usage alone. |
| Protocol client authority | When active, consumes one normalized OpenAPI-derived generated client regardless of backend language, records generator/version/command, and does not create a frontend authority. | Hand-maintains endpoint paths or a second DTO family alongside an active generated client. |
| Protocol semantics | Preserves nullability, optionality, enum, ID, date, money, pagination, auth, success, validation/business error, and compatibility semantics. | Treats successful typecheck as semantic or backend-conformance proof. |
| Protocol ownership gap | Stops inference and hands a missing response/envelope/permission shape to the backend or contract owner while preserving the current adapter boundary. | Reverse-engineers an authoritative backend schema from Axios, a page component, or legacy prose. |
| Client drift and live gate | When active, clean-regenerates without unexplained drift, runs frontend gates, and uses a real consumer loading/success/error check when required; marks unavailable backend/CI evidence `Not verified`. | Claims the OpenAPI workflow is verified from static frontend checks. |
| Desktop boundary | Keeps native access behind IPC/command helpers and routes real-window proof to `ops-client`. | Calls native APIs directly from generic UI or treats browser preview as desktop proof. |
| Validation integrity | Prefers repository-defined non-mutating checks, isolates known writers when practical, compares Worktree state around commands that may write, classifies drift, and restores only when exact pre-state plus complete exclusive attribution prove no concurrent or mixed ownership. | Trusts a command name as proof of non-mutation, hides source rewrites, absorbs validation drift into scope, or restores over concurrent/user-owned work. |
| Validation | Runs matching project commands and uses `ops-browser` or `ops-client` for UI behavior evidence when needed, or marks gaps `Not verified`. | Claims UI correctness without checks or evidence. |
| Target-surface acceptance | Freezes the exact host/container, route, entrypoint, state, and observable acceptance; treats previews and sibling surfaces separately; after an initial check plus one correction recheck fails the same way, stops patching and returns a diagnosis or single-owner handoff with the diff and evidence preserved. | Switches surfaces, continues speculative patches, or calls an alternate preview target acceptance. |
| Nested overlay acceptance | For page-level modal/Drawer/portal ownership changes, verifies outer mask and blocked background, all affected entrypoints, nested selector/popover usability, stacking/clipping, dismissal, and focus restoration in the real target host. | Verifies only the outer Drawer or a standalone preview while nested overlays, background blocking, another entrypoint, or focus restoration remain unchecked. |
| Two-pass visual gate | For selected-source work, compares source/runtime at the same viewport/state, reads computed geometry/style, fixes confirmed findings, then repeats capture and computed checks. | Claims visual completion from one screenshot, SCSS inspection, or build/lint success. |
| Visual runtime coverage | Checks real per-item assets and isolated fallback, native-control font inheritance, truncation, final contrast, section alignment, dimensions, hover/focus, applicable feedback states, desktop target, and every specified breakpoint. | Uses a generic fallback as normal assets or declares completion with required states/breakpoints unchecked. |
| Scope control | Changes only files and lines needed for the frontend request. | Performs unrelated cleanup or broad component reorganization. |

## Scoring

Score each quality case from 0 to 10. Minimum pass: all trigger/non-trigger expectations are correct and every quality case scores at least 8.
