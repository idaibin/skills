interface:
  display_name: "Frontend Implementation"
  short_description: "Implement frontend with traceable visual closure"
  default_prompt: "Use $dev-frontend to map accepted requirements to source owners, implement scoped frontend changes, and run two-pass same-state visual comparison when a selected source controls acceptance."
