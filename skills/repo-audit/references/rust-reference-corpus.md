# Reference Corpus

Reviewed 2026-07-23. Repository links are pinned to the inspected commit.
External sources remain subordinate to the target repository.

| Source | Observed pattern | Adopted rule | Not directly adopted | Reason |
| --- | --- | --- | --- | --- |
| [ripgrep `d5b85d4`](https://github.com/BurntSushi/ripgrep/tree/d5b85d44057ff729a89be9c6549958c45d95aa99) | Root [manifest](https://github.com/BurntSushi/ripgrep/blob/d5b85d44057ff729a89be9c6549958c45d95aa99/Cargo.toml) exposes one CLI binary, responsibility-based member crates, MSRV/edition, narrow features, release debug info, and an explicit LTO profile; integration and mmap behavior have dedicated tests | inspect real workspace/target/profile/feature contracts; keep CLI composition separate; make optimized profiles and behavior tests intentional | ripgrep's exact crate split, allocator target, mmap defaults, profile values, and release process | workload, platform, deployment, and compile-time trade-offs differ |
| [uv `b338fc8`](https://github.com/astral-sh/uv/tree/b338fc8ead7357938c167bebd2df268a6fe31d37) | [AGENTS.md](https://github.com/astral-sh/uv/blob/b338fc8ead7357938c167bebd2df268a6fe31d37/AGENTS.md) requires nearby test patterns, focused validation, explicit safety comments, and scoped lockfile changes; [crate index](https://github.com/astral-sh/uv/blob/b338fc8ead7357938c167bebd2df268a6fe31d37/crates/README.md) describes stable responsibilities and a shared-trait crate used to avoid cycles | inspect analogous tests; justify crate/trait boundaries by responsibility; keep unsafe justification and dependency changes scoped | uv's large workspace, snapshot preference, naming/style choices, or release-build prohibition | these are repository-specific conventions, not universal Rust rules |
| [Tokio `c4c6265`](https://github.com/tokio-rs/tokio/tree/c4c6265a0746a79d4a2f3852f726aa0101f29fd3) | [graceful shutdown](https://github.com/tokio-rs/tokio/blob/c4c6265a0746a79d4a2f3852f726aa0101f29fd3/examples/graceful-shutdown.rs) separates stop signal, task tracking, wait, and bounded client notification; runtime source and benches distinguish blocking work and bounded/unbounded channel behavior | record joined, supervised, intentionally detached, or runtime-owned lifecycle; require cancellation/join/wait for tasks whose responsibility needs them; inspect timeout, blocking, capacity, and overload semantics | mandatory `CancellationToken`, `TaskTracker`, universal joining, Tokio runtime flavor, or one channel capacity | task responsibility, primitives, and sizing belong to the target runtime and workload |
| [Tokio `select!`](https://docs.rs/tokio/latest/tokio/macro.select.html), [`JoinHandle`](https://docs.rs/tokio/latest/tokio/task/struct.JoinHandle.html), and [graceful shutdown](https://tokio.rs/tokio/topics/shutdown) | losing `select!` branches are dropped, cancellation safety is operation-specific, dropping a handle detaches supported work, and shutdown separates detection, notification, and waiting | review branch restart safety and require joining/result observation only when task responsibility needs it; accept bounded observable non-critical detachment | mandatory `JoinSet`/tracker/token, universal handle retention, or silent detachment of critical/resource-owning work | lifecycle policy follows side effects, durability, resources, shutdown, and caller-visible outcomes |
| [Axum extractors](https://docs.rs/axum/latest/axum/extract/), [`State`](https://docs.rs/axum/latest/axum/extract/struct.State.html), and [`IntoResponse`](https://docs.rs/axum/latest/axum/response/trait.IntoResponse.html) | typed extractors model request boundaries, request bodies have one consumer, application state composes through typed state, and outer errors map to responses | verify extractor/body order, state ownership, rejection/error mapping, middleware scope, and router-service tests | mandatory `Arc<AppState>`, `Extension`, permissive CORS, or one timeout | handler/state/layer choices depend on trust, workload, and repository architecture |
| [Tauri capabilities](https://v2.tauri.app/security/capabilities/), [`AppManifest::commands`](https://docs.rs/tauri-build/latest/tauri_build/struct.AppManifest.html#method.commands), [scope](https://v2.tauri.app/reference/acl/scope/), and [CSP](https://v2.tauri.app/security/csp/) | capabilities authorize core/plugin permissions, while custom `invoke_handler` commands are app-wide by default until app-command permissions are generated and assigned; scopes constrain resources through command/plugin enforcement, domain policy may still require Rust-owned user authorization, and CSP is defense in depth | distinguish custom commands; verify `build.rs`, generated allow/deny permissions, capability assignment, scope enforcement, domain authorization, input validation, CSP, and caller evidence | assuming custom commands are capability-gated by default, trusting frontend types, duplicating platform ACL in every command, treating CSP as authorization, or arbitrary path/shell access | the layers have different owners and application policy remains repository-specific |
| [Loom `948c8cc`](https://github.com/tokio-rs/loom/tree/948c8cc78b178ede6eeff3afc7d97f2f4ea08559) | [README](https://github.com/tokio-rs/loom/blob/948c8cc78b178ede6eeff3afc7d97f2f4ea08559/README.md) models permutations through Loom synchronization types and documents incomplete memory-model coverage | use Loom for small represented concurrency state machines and report limits | treating a passing model as proof of hidden OS, FFI, or unmodeled synchronization | Loom sees only represented operations and has explicit limitations |
| [rusqlite `77a1719`](https://github.com/rusqlite/rusqlite/tree/77a171935741ce56da0bb67277068cb0c08aaa85) | [README](https://github.com/rusqlite/rusqlite/blob/77a171935741ce56da0bb67277068cb0c08aaa85/README.md) makes bundled/system linkage and supported SQLite versions explicit; API examples keep connection, statement, row iterator, and mapping lifetimes visible | verify actual linkage/runtime and keep local connection/statement/rows lifetime explicit | enabling `bundled` for every library or using one connection pattern everywhere | generic libraries and deployment-controlled apps have different linkage constraints |
| [SQLx `1d674f5`](https://github.com/launchbadge/sqlx/tree/1d674f51581598f55436451d5b4b73100cae0b56) | [README](https://github.com/launchbadge/sqlx/blob/1d674f51581598f55436451d5b4b73100cae0b56/README.md) documents optional compile-time checked queries, runtime/features, pooling, row streaming, statement caches, transactions, and SQLite FFI boundary | evaluate SQLx from existing async/query/pool/streaming needs and enabled features | migrating to SQLx as a universal modernization or treating SQLite as nonblocking | an async API still interacts with embedded SQLite and project-specific worker/connection design |
| [Tantivy `6b8bd7b`](https://github.com/quickwit-oss/tantivy/tree/6b8bd7b8847c87686117a0e3a400bd6377afb8fd) | [index writer](https://github.com/quickwit-oss/tantivy/blob/6b8bd7b8847c87686117a0e3a400bd6377afb8fd/src/indexer/index_writer.rs) uses bounded document channels, worker handles, memory budgets, commit/rollback, and explicit close behavior; [`OwnedBytes`](https://github.com/quickwit-oss/tantivy/blob/6b8bd7b8847c87686117a0e3a400bd6377afb8fd/ownedbytes/src/lib.rs) documents zero-copy slices retaining full backing memory; hot unsafe code carries a safety comment and tests/benches | review bounded pipelines, worker/index lifecycle, retained backing allocations, measured hot paths, and local unsafe invariants | Tantivy's memory budgets, mmap/index architecture, thread count, nightly benches, or unsafe optimization | search-index workloads and invariants do not transfer automatically |
| [Rust API Guidelines](https://rust-lang.github.io/api-guidelines/) | Guidelines emphasize interoperable public APIs, validation, newtypes, debugability, future-proofing, and nonblocking/fallible teardown alternatives | make public invariants explicit and keep fallible/blocking cleanup available outside `Drop` | treating every checklist item as a mandate | the source explicitly presents guidelines, not universal requirements |
| [Rust Performance Book](https://nnethercote.github.io/perf-book/) | [profiling](https://nnethercote.github.io/perf-book/profiling.html), [benchmarking](https://nnethercote.github.io/perf-book/benchmarking.html), and [heap allocation](https://nnethercote.github.io/perf-book/heap-allocations.html) guidance starts from hot paths and representative workloads; clone and `SmallVec` decisions require profiling | measure first; compare same workload; use allocation evidence; keep simple clones when cost is irrelevant | fixed profiler, allocator, container, or universal improvement threshold | tools, platforms, and meaningful variance differ |
| [Cargo Book](https://doc.rust-lang.org/cargo/) | [workspaces](https://doc.rust-lang.org/cargo/reference/workspaces.html), [Rust version policy](https://doc.rust-lang.org/cargo/reference/rust-version.html), [features](https://doc.rust-lang.org/cargo/reference/features.html), [profiles](https://doc.rust-lang.org/cargo/reference/profiles.html), and [build performance](https://doc.rust-lang.org/cargo/guide/build-performance.html) define root-owned settings, explicit member inheritance, MSRV trade-offs, and build configuration | inspect actual inheritance and choose a documented toolchain/MSRV policy; treat edition 2024/resolver 3 as current options rather than forced migration | a fixed moving Rust release, universal MSRV, LTO, codegen-unit, or feature policy | target repository and user compatibility determine the baseline |
| [Clippy](https://doc.rust-lang.org/clippy/) | Lint groups have different defaults and false-positive trade-offs; usage supports scoped configuration and repository policy | run repository-defined Clippy gates and justify narrow suppressions | enabling every pedantic/restriction lint or auto-fixing unrelated code | lint policy and MSRV are repository contracts |
| [Clippy lint list](https://doc.rust-lang.org/clippy/lints.html) | correctness, suspicious, style, complexity, performance, pedantic, nursery, cargo, and restriction groups carry different intent and default levels | record the group and selected feature/target context before treating output as audit evidence | enabling the whole restriction group or treating one configuration as whole-crate reachability | optional groups and conditional compilation need repository-owned policy and a feature/target matrix |
| [Miri](https://github.com/rust-lang/miri) | Miri detects many UB classes and some leaks/data races but has OS, FFI, network, layout, and interleaving limitations | use Miri for supported unsafe tests and report unsupported paths | treating Miri as complete proof or a native/FFI runtime test | official limitations exclude important production surfaces |
| [RustSec](https://github.com/rustsec/rustsec) | `cargo audit` checks lockfiles against the RustSec Advisory Database | include known-advisory checking when available and relevant | claiming a clean audit proves dependency or application security | advisory coverage and reachability are incomplete |
| [SQLite WAL](https://sqlite.org/wal.html) | WAL normally checkpoints automatically, but disabled checkpoints, long readers, and large transactions can grow the WAL; busy outcomes remain possible | inspect readers, transactions, checkpoint policy, writer coordination, runtime version, and recovery | treating WAL as multi-writer or deleting sidecar files | WAL changes concurrency, not SQLite's single-writer constraint |
| [SQLite query planning](https://sqlite.org/eqp.html) | `EXPLAIN QUERY PLAN` distinguishes scans/searches, index terms, covering indexes, join nesting, and temporary B-trees; output format is not stable API | validate critical queries with representative data and interpret plans in context | parsing plan text in application code or declaring every scan a defect | broad/tiny-table scans can be correct and output may change |
| [SQLite pragmas and VACUUM](https://sqlite.org/pragma.html) | freelist pages are reusable; auto-vacuum modes have trade-offs; optimize guidance depends on connection lifetime; [VACUUM](https://sqlite.org/lang_vacuum.html) rebuilds the DB and can require substantial extra space | measure page/freelist/file state and assess disk, locks, outage, and benefit before maintenance | default `auto_vacuum=FULL`, scheduled full vacuum, or arbitrary cache/mmap increases | maintenance cost and value depend on workload and deployment |

## Explicit Local Governance Decisions

- Apply the rule priority in `SKILL.md`.
- Require targeted repository and analogous-code investigation before creating
  public traits, global state, runtimes, pools, caches, or database layers.
- Distinguish portable governance from organization baselines, new-project
  templates, repository contracts, and legacy exceptions.
- Treat version numbers and directory trees as scoped baselines or templates,
  not universal Rust rules.
- Prefer local-first, single-binary, minimal-dependency designs when they already
  satisfy the product contract.
- Treat file length, `clone`, `Arc<Mutex<_>>`, `unwrap`, unsafe, and full scans as
  evidence prompts, not automatic findings.
- Require architecture, migration, performance, and operational documentation to
  change with the source-of-truth boundary.
- Require add, reuse, move, rename, and delete operations to update the complete
  affected manifest, registration, test, migration, deployment, docs, and index
  chain, while preserving historical migrations needed by installed versions.

These are reusable engineering governance decisions supported by the corpus,
not claims that Rust or SQLite mandates one folder structure or framework.
