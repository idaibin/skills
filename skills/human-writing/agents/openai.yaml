interface:
  display_name: "Human Writing"
  short_description: "Draft, rewrite, diagnose, or adapt writing; not translation-only"
  default_prompt: "Use $human-writing to draft, rewrite, diagnose, or adapt prose while preserving language, facts, attribution, and uncertainty; exclude translation-only, and use a private English outline for Chinese only when helpful."
