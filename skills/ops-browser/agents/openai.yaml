interface:
  display_name: "Ops Browser"
  short_description: "Operate browser surfaces with verified computed evidence"
  default_prompt: "Use $ops-browser to prefer the non-interrupting Codex in-app Browser when suitable, verify capability, identity, target, and authorization, then perform the smallest browser action or same-state visual comparison using only exposed browser or DevTools evidence; return direct before/after and computed evidence with unsupported claims marked Not verified."
