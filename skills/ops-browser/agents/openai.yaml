interface:
  display_name: "Ops Browser"
  short_description: "Operate browser surfaces with verified state and actions"
  default_prompt: "Use $ops-browser to prefer the non-interrupting Codex in-app Browser when suitable, select only exposed browser or DevTools evidence, verify identity, target, and authorization, and return before/after evidence with unsupported claims marked Not verified."
