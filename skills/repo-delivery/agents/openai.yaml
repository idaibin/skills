interface:
  display_name: "Repository Delivery"
  short_description: "Preserve progress and deliver authorized Git changes"
  default_prompt: "Use $repo-delivery to preserve explicitly authorized task progress with semantic commits, normalize completed task-branch history safely, deliver reviewed Git changes, and prove final local and remote state."
